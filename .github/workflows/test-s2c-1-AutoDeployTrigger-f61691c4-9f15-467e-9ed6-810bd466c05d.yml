name: Trigger auto deployment for test-s2c-1

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/test-s2c-1-AutoDeployTrigger-f61691c4-9f15-467e-9ed6-810bd466c05d.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.TESTS2C1_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.TESTS2C1_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.TESTS2C1_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.TESTS2C1_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.TESTS2C1_REGISTRY_PASSWORD }}
          containerAppName: test-s2c-1
          resourceGroup: oryx-williamhe
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          


