name: Trigger auto deployment for test-ca-williamhe-west-us

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/test-ca-williamhe-west-us-AutoDeployTrigger-f64a6fc5-7efe-4bd5-975c-788bf1ed48dc.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.TESTCAWILLIAMHEWESTUS_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.TESTCAWILLIAMHEWESTUS_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.TESTCAWILLIAMHEWESTUS_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.TESTCAWILLIAMHEWESTUS_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.TESTCAWILLIAMHEWESTUS_REGISTRY_PASSWORD }}
          containerAppName: test-ca-williamhe-west-us
          resourceGroup: oryx-williamhe
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          


