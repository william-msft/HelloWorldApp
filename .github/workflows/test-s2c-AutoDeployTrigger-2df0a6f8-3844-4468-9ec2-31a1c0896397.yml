name: Trigger auto deployment for test-s2c

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/test-s2c-AutoDeployTrigger-2df0a6f8-3844-4468-9ec2-31a1c0896397.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.TESTS2C_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.TESTS2C_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.TESTS2C_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.TESTS2C_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.TESTS2C_REGISTRY_PASSWORD }}
          containerAppName: test-s2c
          resourceGroup: test-s2c-using-existing-env
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          


